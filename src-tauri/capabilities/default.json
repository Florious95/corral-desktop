{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "No native commands are exposed; the client uses browser APIs only.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
