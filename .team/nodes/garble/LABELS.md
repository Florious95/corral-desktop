# Ground truth · 用户 2026-08-23 亲自看图打标

来源：`.team/nodes/garble/shots-web/`（59 张，1400x860，CSS 视口与桌面端一致，整界面含左侧 UI）。
🔴 **这是唯一权威标签。⛔ 不许用任何我们自己写的 detector 覆盖或"修正"它。**

用户原话：「3.6.35.36.58 这么5张存在问题。」

## 错排（5）

| # | 会话 | socket | pane | 主机几何 |
|---|---|---|---|---|
| 03 | reviewer-r19 | ta-a9fd5b7defbd | %100 | 235x50 |
| 06 | reviewer-r21 | ta-a9fd5b7defbd | %103 | 235x50 |
| 35 | w2-dev-b | ta-a0afa5f9c7f6 | %7 | 235x50 |
| 36 | w2-dev-c | ta-a0afa5f9c7f6 | %8 | 235x50 |
| 58 | grok | ta-5674137b752d | %0 | 140x40 |

## 正常（54）——关键对照组

**同一 socket `ta-a9fd5b7defbd`、同样 235x50、相邻 pane，坏好交替：**

| pane | 会话 | 标签 |
|---|---|---|
| %100 | reviewer-r19 | **坏** |
| %101 | tester-t152 | 好 |
| %102 | reviewer-r20 | 好 |
| %103 | reviewer-r21 | **坏** |
| %104 | tester-t153 | 好 |

⇒ **几何彻底出局。** 差异在内容或会话状态。

## leader 已量过、⛔ 没有分辨力的东西（不要重做）

| 量具 | 结果 |
|---|---|
| `CR` 计数 | 坏/好 **全是 0** |
| `CUP` 序列计数 | 坏/好 **全是 0** |
| alt-screen 标记 | 坏/好 **全是 0** |
| 「非空行数」 | 坏 8/8/6/18 vs 好 35/37/37/42 —— 看似相关，但坏的 `%7` 是 **38**，**⛔ 反例存在** |
| 每行起始列分布 | 坏/好 **看不出差别**（那只是内容缩进） |

## 已被证伪的旧说法（⛔ 不许再当前提）

- 「宽主机 235 是必要条件」——`%0 default` 也是 235，永远正常；`grok` 是 140 却坏。
- 「捕获字节本身有问题」——用户确认 79 张按主机几何渲染的参考图**全部正常**。
- 我们自己那套 `detectGarble` 启发式给的全部标签（79 错乱/471 正常等）——**循环论证，已作废**。


---

## 追加（2026-08-23 晚）：**存在第二种失效模式**

用户在客户端里重新扫了一遍，确认上面那 5 个「一打开就坏」属实。**但另有一组不同性质的**：

> 「这几个第一次看它是排布好的，就和你截图的一样，它们是好的。但是你**来回轮转**地去看它的界面、
> 去看它的会话，结果就错排了。」

用户圈出的一组（截图 `image #160` 红框）：
`tester-t150` / `reviewer-r16` / `reviewer-r17` / `tester-t151` / `reviewer-r18`（均在 `多agent协作`，235x50）。

### 两种模式（⛔ 不要混为一谈）

| 模式 | 触发 | 名单 | 我的巡检抓不抓得到 |
|---|---|---|---|
| **一：一打开就坏** | 首次点开 | reviewer-r19 / reviewer-r21 / w2-dev-b / w2-dev-c / grok | ✅ 单次巡检即可 |
| **二：轮转后坏** | 反复切换、**停留时间短** | tester-t150 / reviewer-r16 / reviewer-r17 / tester-t151 / reviewer-r18 | ❌ **单次巡检原理上抓不到** |

### 模式二已自动复现（leader 2026-08-23 实测，⛔ 不是推断）

| 条件 | 结果 |
|---|---|
| 均匀轮询 59 个，停留 **2200ms**，跑 3 轮 | `reviewer-r17` 第 1/2/3 轮**全好** |
| `reviewer-r17` ↔ `reviewer-r16` 来回快切，停留 **600ms** | **第 15 次明显错排** |

⇒ **停留时间是自变量。** 2.2 秒够它稳住，600ms 不够。
复现命令：`node .team/nodes/garble/toggle-web.mjs --a reviewer-r17 --b reviewer-r16 --n 16 --dwell 600 --out <目录>`
证据图：`.team/nodes/garble/toggle/k15__reviewer-r17.png`（坏）对 `.team/nodes/garble/shots-rounds/r3__25__reviewer-r17.png`（好）。

🔴 **该复现是在 `main = a2515bd`（含 PR #80 同宽不变量修复）上做的**，运行中 `.app` md5 `f5f76793966dd735dfd71161714ff091` 已核 ⇒ **#80 没有覆盖快速切换这条路径。**

## ⛔ 测试禁区（2026-08-23 用户令）

> 「要注意，我收藏里面的那些会话，在测试过程中都不要点。」

收藏 = 5 个 `claude_code` 席位（多agent协作 / tmux桌面端 / 本地部署 / 讨论team-agent / 远程Agent安卓），
**含用户正在用的那条对话**。两个夹具已内置 `EXCLUDE` 按标题跳过。⛔ 不许移除。

---

## 🔴 2026-08-23 更新：旧样本已随席位轮换消失，用户给了**新的三个**

只读 `list-panes` 核过（⛔ 未订阅、未 reshape）：`reviewer-r19` / `reviewer-r21` / `tester-t152`
**已经不存在**（那个 workspace 的席位干完一轮就轮换）。⇒ **上面基于它们的对照组作废。**
`w2-dev-b` / `w2-dev-c` / `grok` 仍在。

### 用户指定的新样本（2026-08-23，截图 image #166）

> 「你通过这三个就可以。**相互点就可以发现冲突的问题、错乱的问题**。」

| 会话 | socket | pane | 几何 | 列表上的状态点 |
|---|---|---|---|---|
| `reviewer-r24` | `ta-a9fd5b7defbd` | `%107` | 235x50 | 空心（空闲） |
| `reviewer-r25` | `ta-a9fd5b7defbd` | `%108` | 235x50 | 空心（空闲） |
| `tester-t154`  | `ta-a9fd5b7defbd` | `%109` | 235x50 | **绿点（活跃）** |

✅ **这三个用户明确授权可以点**（⛔ 收藏那 5 个 `claude_code` 仍然一律不许点）。
🔴 三者**同 socket、同几何、相邻 pane** ⇒ 又一组几何被控住的对照组，且**其中两空闲一活跃**。

### ⛔ 标签会腐烂——已做的补救

原始字节已只读固化到 `.team/nodes/garble/goldbytes/`（`capture-pane -e -p`，⛔ 未订阅）：
`reviewer-r24`(5728B) / `reviewer-r25`(5738B) / `tester-t154`(4996B) / `w2-dev-b`(17612B) / `w2-dev-c`(2739B)。
🔴 **该目录 = pane 正文，已进 `.gitignore`，⛔ 永不入库**（§一点八）。
⇒ 以后席位再轮换，**标签不再随之蒸发**。

**教训**：用户打标那一刻就该把字节存下来，我只存了图。图后来因泄漏事故不能入库，标签就成了孤儿。
